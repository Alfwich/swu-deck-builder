# Card catalog source (keep the real endpoints in your untracked .env file).
SWU_DB_API_BASE_URL=https://example.invalid
SWU_DB_SETS_PAGE_URL=https://example.invalid

# Local application server.
APP_SERVER_HOST=127.0.0.1
APP_SERVER_PORT=8787

# Optional OpenAI-assisted deck generation. The API key is read only by the
# local Node server and is never exposed through Vite or sent to the browser.
AGENTIC_DECK_GENERATION_ENABLED=false
SWU_OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra
OPENAI_REASONING_EFFORT=medium
OPENAI_MAX_OUTPUT_TOKENS=4000
OPENAI_REQUEST_TIMEOUT_MS=120000
OPENAI_STORE_RESPONSES=false

# Optional: reuse a catalog file that was uploaded to the OpenAI Files API.
# When empty, the server uploads and caches one automatically.
OPENAI_CATALOG_FILE_ID=
